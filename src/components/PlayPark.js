import React from "react";

export const PlayPark = () => {
  return (
    <section id="play-park">
      <div className="fixed top-0 left-0 z-50 w-full h-full overflow-hidden transition-transform bg-gray-900 bg-opacity-50">
        <div className="fixed w-44 h-[600px] ml-[50%] bg-white overflow-auto">
          sdf
        </div>
      </div>
    </section>
  );
};
